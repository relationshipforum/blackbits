.page-header
  %h1 Signature

.col-sm-10
  = form_for current_user, { html: { class: "form-horizontal" } } do |f|
    .row.form-group
      = f.text_area :signature, class: "form-control markdown", rows: 10
    .row.text-right
      %button.btn.btn-primary Submit
